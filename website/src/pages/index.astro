---
import Layout from "../layouts/Layout.astro";
import LandingContent from "../components/LandingContent.astro";
import { t } from "../i18n/utils";
const locale = "en";
---
<Layout title={t(locale, "hero.title")} locale={locale}>
  <LandingContent locale={locale} />
</Layout>
